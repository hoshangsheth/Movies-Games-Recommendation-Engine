# Base URL for the FastAPI backend. In local dev, Vite proxies /api to
# http://localhost:8000 (see vite.config.js), so this can usually be left
# unset. Set it explicitly when the frontend and backend are deployed to
# different origins.
VITE_API_BASE_URL=/api/v1
